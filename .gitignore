# ---- Private planning / design set (never publish) ----
# Maintained privately per README — planned/unreleased features stay off the
# public repo. Keep these local-only; never `git add -f` them.
Build-Prompts-Guide.md
prd.md
product-roadmap.md
product-vision.md
vision.json
gtm.md
Freally-Capture-Competition-Guide-Features.md
GO-LIVE-DRILL.md
*[Pp]rompt*.md

# ---- Rust ----
/target
**/target
# Cargo.lock IS committed (app workspace — reproducible builds + cargo-audit)
*.pdb

# ---- Tauri ----
/src-tauri/target
/src-tauri/gen
/src-tauri/WixTools

# ---- Node / Vite UI ----
/ui/node_modules
/ui/dist
/ui/.vite
node_modules
dist
.vite
*.tsbuildinfo

# ---- Editors / OS / agents ----
.claude/
.vscode/*
!.vscode/extensions.json
.idea
*.swp
.DS_Store
Thumbs.db

# ---- Local artifacts (never commit) ----
# recordings, replays, exported clips, fetched binaries/models
*.frec
*.mp4
*.mkv
*.mov
*.webm
/recordings
/replays
# on-demand, non-bundled components fetched to local caches
/.cache/ffmpeg
/.cache/models
*.gguf
*.onnx

# ---- Secrets / local config ----
.env
.env.*
*.key
*.pem
# local user config / stream keys must never be committed
/local
secrets.json

# Tauri updater signing key (private — never commit; public key lives in tauri.conf.json)
.tauri/

# Local drill runbook (not shipped)
Steps-Drills.md

# Playwright UI visual-smoke: keep the harness (config/spec/mock), ignore outputs
ui/e2e/screenshots/
ui/e2e/.output/
ui/test-results/
ui/playwright-report/
/playwright-report/
/test-results/

# Local QA working doc (not shipped)
Live-To-Do-List.md

# CAP-M feature spec + batch trackers / handoffs (local working docs, not shipped)
Freally-Capture-Feature-Roadmap.md
BATCH-1-PROGRESS.md
BATCH-2-HANDOFF.md
BATCH-2-PROGRESS.md
BATCH-3-HANDOFF.md
BATCH-3-PROGRESS.md
CAPN-RUN-PROGRESS.md
NEXT-SESSION-HANDOFF.md
PHASE-8-HANDOFF.md
PHASE-4-PROGRESS.md
PHASE-5-PROGRESS.md
PHASE-6-PROGRESS.md
